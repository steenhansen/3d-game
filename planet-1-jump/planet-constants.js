const RECOIL_COUNTDOWN = 5;   // was 30


const TRAVEL_SPEED = 8;   //1sluggish  4slow  8normal 16fast  32extreme

function singularPlanetCode(a_mess) {
  all_dead = enemiesAllZombies(g_enemy_list);
  if (all_dead) {
    exit_sign.p_sign_text_col = 'lime';
    exit_sign.o_url_hit = 'planet-2-carom/index.html';
  }
}